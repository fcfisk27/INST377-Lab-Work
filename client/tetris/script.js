document.addEventListener('DOMContentLoaded', () => {
  const grid = document.querySelector('.grid')
  let squares = ArrayFrom(document.querySelectorAll('.grid div'))
  const width = 10
  console.log(squares);
});